@{
    ViewData["Title"] = Model.Title;
}
@model TMT_Requirements
<link href="~/edit/edit.css" rel="stylesheet" />
<script src="~/edit/edit.js"></script>
<div class="document_body">
    <div class="left">
        <div class="ui transparent input fluid" style="margin-bottom: 20px; ">
            <input type="text" id="Title" placeholder="需求标题..." value="@Model.Title" readonly="readonly" style="font-size: 28px; font-weight: bold !important; " autocomplete="off">
        </div>
        <div class="context_edit">
            @Html.Raw(Model.Detailes.FirstOrDefault(c => c.Version == Model.NowVersion).Content)
        </div>
    </div>
    <div class="right">
        <div class="ui form tiny">
            <div class="field">
                <label>版本</label>
                <div class="ui floating labeled icon dropdown button" id="setVersion" data-value="@Model.RequirementID">
                    <input type="hidden" value="@Model.NowVersion">
                    <i class="code branch icon"></i>
                    <span class="text">Add User</span>
                    <div class="menu">
                        @foreach (var item in Model.Detailes)
                        {
                            <div class="item" data-value="@item.Version"># @(item.Version)</div>
                        }
                    </div>
                </div>
            </div>
            <div class="field">
                <label>优先级</label>
                <div class="ui disabled rating" id="EmergencyLevel" data-icon="star" data-rating="@Model.EmergencyLevel" data-max-rating="5"></div>
            </div>
            <div class="field">
                <label>项目</label>
                <div class="ui label basic">
                    <i class="briefcase icon"></i> @G.NowProject.ProjectName
                </div>
            </div>
            <div class="field">
                <label>类型</label>
                <div class="ui label basic">
                    @Model.MType
                </div>
            </div>
            <div class="field">
                <label>模块</label>
                <div class="ui label basic">
                    @Model.Module.ModuleName
                </div>
            </div>
            <div class="field">
                <label>评审</label>
                <div class="ui image label basic">
                    <img src="@Model.AuditorUser.Avatar">
                    @Model.AuditorUser.UserName
                </div>
            </div>
            @if (Web.Test.Requirement.AllowEdit(Model, G.User.UserID))
            {
                <a class="ui tiny blue button" href="/Requirement/Add?RequirementID=@Model.RequirementID">
                    <i class="edit icon"></i>
                    编辑
                </a>
            }
            @if (Web.Test.Requirement.AllowSubmit(Model, G.User.UserID))
            {
                <button class="ui tiny teal button" onclick="page_action.submit('@Model.RequirementID')">
                    <i class="share square outline icon"></i>
                    发布
                </button>
            }
            @if (Web.Test.Requirement.AllowReview(Model, G.User.UserID))
            {
                <button class="ui tiny yellow button" onclick="page_action.review('@Model.RequirementID')">
                    <i class="stamp icon"></i>
                    审核
                </button>
                <button class="ui tiny brown button" onclick="page_action.handOver('@Model.RequirementID')">
                    <i class="random icon"></i>
                    转审
                </button>
            }
            @if (Web.Test.Requirement.AllowSealUP(Model, G.User.UserID))
            {
                <button class="ui tiny grey button" onclick="page_action.sealUP('@Model.RequirementID')">
                    <i class="sign in alternate icon"></i>
                    归档
                </button>
            }
            @if (Web.Test.Requirement.AllowSubmit(Model, G.User.UserID))
            {
                <button class="ui tiny red button" onclick="page_action.delete('@Model.RequirementID')">
                    <i class="trash alternate outline icon"></i>
                    删除
                </button>
            }
        </div>
        <div class="ui horizontal divider">
            <i class="tasks icon"></i>
            任务
        </div>
        @if (Model.Tasks.Count > 0)
        {
            <div class="ui comments mini">
                @foreach (var item in Model.Tasks)
                {
                    <div class="comment">
                        <div class="avatar">
                            <img src="@item.ExecutorUser.Avatar">
                        </div>
                        <div class="content">
                            <span class="author">@item.ExecutorUser.UserName</span>
                            <div class="text" style="margin-top:.1em;">
                                @switch (item.Status)
                                {
                                    case DBEnums.TasksStatus.进行:
                                        <i class="play circle outline icon orange" style="font-size: 12px;margin: 0px;" title="@item.Status"></i>
                                        break;
                                    case DBEnums.TasksStatus.完成:
                                        <i class="check circle outline icon green" style="font-size: 12px;margin: 0px;" title="@item.Status"></i>
                                        break;
                                    case DBEnums.TasksStatus.取消:
                                        <i class="stop circle outline icon red" style="font-size: 12px;margin: 0px;" title="@item.Status"></i>
                                        break;
                                }
                                <a>@item.Title</a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <span class="ui grey text">无</span>
        }
        <div class="ui horizontal divider">
            <i class="history icon"></i>
            记录
            @if (Web.Test.Requirement.AllowAddLog(Model, G.User.UserID))
            {
                <a class="ui label mini addlog basic" onclick="page_action.addlog('@Model.RequirementID')">
                    <i class="plus icon"></i>
                </a>
            }
        </div>
        <div class="ui comments mini">
            @foreach (var item in Model.Logs.OrderBy(c => c.CreateDate))
            {
                <div class="comment">
                    <div class="avatar">
                        <img src="@item.User.Avatar">
                    </div>
                    <div class="content">
                        <span class="author">@item.User.UserName</span>
                        <div class="metadata">
                            <span class="date">@item.CreateDate</span>
                        </div>
                        <div class="text">
                            @Html.Raw(item.Content)
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
<script>
    var page_action = {
        edit_obj: null,
        EmergencyLevel: $('#EmergencyLevel'),
        init: () => {
            page_action.edit();
            page_action.EmergencyLevel.rating({
                onRate: (value) => {
                    page_action.EmergencyLevel.removeClass('green olive yellow orange red grey');
                    switch (value) {
                        case 1:
                            page_action.EmergencyLevel.addClass('green');
                            break;
                        case 2:
                            page_action.EmergencyLevel.addClass('olive');
                            break;
                        case 3:
                            page_action.EmergencyLevel.addClass('yellow');
                            break;
                        case 4:
                            page_action.EmergencyLevel.addClass('orange');
                            break;
                        case 5:
                            page_action.EmergencyLevel.addClass('red');
                            break;
                    }
                }
            }).rating('set rating', page_action.EmergencyLevel.rating('get rating'));
            $('#setVersion').dropdown({
                onChange: function (value, text, $choice) {
                    var id = $choice.parents('.dropdown').data('value');
                    window.location.href = "/Requirement/View?RequirementID=" + id + "&Version=" + value;
                }
            });
        },
        edit: () => {
            page_action.edit_obj = new editor('.context_edit', {
                edit: false,
                upload: {
                    url: '/Home/UPload',
                    success: (result) => { },
                    error: (error) => { }
                }
            });
        },
        submit: (id) => {
            if (event && event.currentTarget) {
                var target = event.currentTarget;
                modal.ask('确认将此需求提交审核吗？', () => {
                    ajax.post('/Requirement/Submit', { RequirementID: id }, () => { $(target).load_show(); }, (success, data) => {
                        $(target).load_hide();
                        if (success && data.Success) {
                            window.location.reload();
                        }
                    });
                });
            }
        },
        review: (id) => {
            modal.load('/Requirement/Review?RequirementID=' + id, 400);
        },
        sealUP: (id) => {
            if (event && event.currentTarget) {
                var target = event.currentTarget;
                modal.ask('确认归档此需求吗？归档后将锁定需求不再可编辑。', () => {
                    ajax.post('/Requirement/SealUP', { RequirementID: id }, () => { $(target).load_show(); }, (success, data) => {
                        $(target).load_hide();
                        if (success && data.Success) {
                            window.location.reload();
                        }
                    });
                });
            }
        },
        delete: (id) => {
            if (event && event.currentTarget) {
                var target = event.currentTarget;
                modal.ask('确认删除此需求吗？', () => {
                    ajax.post('/Requirement/Delete', { RequirementID: id }, () => { $(target).load_show(); }, (success, data) => {
                        $(target).load_hide();
                        if (success && data.Success) {
                            window.location.href = "/Requirement/Index";
                        }
                    });
                });
            }
        },
        handOver: (id) => {
            modal.load('/Requirement/HandOver?RequirementID=' + id, 400);
        },
        addlog: (id) => {
            modal.load('/Requirement/AddLog?RequirementID=' + id, 400);
        },
    };
    page_action.init();
</script>